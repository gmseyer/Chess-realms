using UnityEngine;

public class FireMarkerLavaTile : MonoBehaviour
{
    // This component marks a lava tile as being created from a fire marker capture
    // It will be destroyed along with the lava tile when both pieces are destroyed
}
